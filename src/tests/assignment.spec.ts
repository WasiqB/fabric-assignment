import { test, expect } from '@playwright/test';

test.describe('UI scenario', async () => {
  test('Navigate to Para bank app', ({ page }) => {

  });
  test('Create new user', ({ page }) => {

  });
  test('Login with new user', ({ page }) => {
    
  });
  test('Verify global menu working', ({ page }) => {
    
  });
  test('Create savings account', ({ page }) => {
    
  });
  test('Validate accounts overview page', ({ page }) => {
    
  });
  test('Transfer funds between accounts', ({ page }) => {
    
  });
  test('Pay the bill with new accounts', ({ page }) => {
    
  });
});

test.describe('API scenario', async () => {
  test('Search transaction by amount', ({ request }) => {

  });
  test('Validate details displayed', ({ request }) => {
    
  });
});
